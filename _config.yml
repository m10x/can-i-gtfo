remote_theme: pages-themes/hacker@v0.2.0
plugins:
- jekyll-remote-theme

title: "Can I GTFO?"
description: "Kubernetes RBAC Exploit Database"

collections:
  entries:
    output: true
    permalink: /:collection/:name/

defaults:
  - scope:
      path: ""
      type: "entries"
    values:
      layout: "entry"
